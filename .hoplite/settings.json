{
  "version": 1,
  "ports": {"preview": 3000, "additional": {}},
  "scripts": {
    "setup": {
      "enabled": true,
      "command": "apt-get update -qq && (git lfs version >/dev/null 2>&1 || apt-get install -y -qq git-lfs) || true; npm ci --no-audit --no-fund || npm install --no-audit --no-fund"
    },
    "run": {"enabled": true, "command": null},
    "archive": {"enabled": true, "command": null},
    "check": {"enabled": true, "command": null}
  },
  "mcpServers": []
}
